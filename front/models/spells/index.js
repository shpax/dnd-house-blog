import createStorage from "../LocalStorage";

export const classStorage = createStorage("spell-class", "");
export const levelStorage = createStorage("spell-level", "");
export const schoolStorage = createStorage("school-level", "");
